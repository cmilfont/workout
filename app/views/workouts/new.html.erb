<h1>New workout</h1>

<% form_for(@workout) do |f| %>
  <%= f.error_messages %>

  <%= hidden_field :workout, :user_id, :value => current_user.id %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', workouts_path %>

